# Dev server listens on all interfaces so phones can open http://YOUR-IP:3000
HOST=0.0.0.0

# API URL is auto-detected: same hostname as the browser, port 8080.
# Uncomment only if you need a fixed override:
# REACT_APP_API_URL=http://localhost:8080
# REACT_APP_API_PORT=8080
